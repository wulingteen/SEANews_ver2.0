# OpenAI API 配置
OPENAI_API_KEY=

# 服务器端口（本地开发用，生产环境由 Zeabur 自动注入）
PORT=8787

# 前端 API 地址（仅本地开发使用）
VITE_API_URL=http://localhost:8787

# Email 配置 (SMTP)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_ADDRESS=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# 应用登录凭证
APP_USERNAME=
APP_PASSWORD=
APP_SECRET_KEY=

# 搜尋新聞後是否將結果索引到 RAG（會觸發大量 embedding API 呼叫）
# 0 = 關閉（預設，速度快） / 1 = 開啟（可做 RAG 檢索）
AGNO_INDEX_WEB_SEARCH_DOCS=0

# 说明：
# 1. 复制此文件为 .env 并填入真实值
# 2. 不要将 .env 文件提交到 Git（已在 .gitignore 中）
# 3. Gmail 需使用"应用专用密码"，不是普通登录密码
#    获取方式：Google 账户 → 安全性 → 两步验证 → 应用专用密码
# 4. 生产环境部署时，在 Zeabur 控制台设置这些环境变量
