# OpenAI API 配置
OPENAI_API_KEY=

# 伺服器埠（本機開發用，生產環境由 Zeabur 自動注入）
PORT=8787

# 前端 API 位址（仅本機開發使用）
VITE_API_URL=http://localhost:8787

# Google OAuth（前後端都需設定同一組 Web Client ID）
VITE_GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_ID=
# 限制可登入網域（可選，逗號分隔，例如：example.com,cathaybk.com.tw）
GOOGLE_ALLOWED_DOMAINS=

# Email 設定 (SMTP)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_ADDRESS=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# 應用登入憑證
APP_USERNAME=
APP_PASSWORD=
APP_SECRET_KEY=

# 搜尋新聞後是否將結果索引到 RAG（會觸發大量 embedding API 呼叫）
# 0 = 關閉（預設，速度快） / 1 = 開啟（可做 RAG 檢索）
AGNO_INDEX_WEB_SEARCH_DOCS=0

# 說明：
# 1. 複製此檔案为 .env 并填入真實值
# 2. 不要將 .env 檔案提交到 Git（已在 .gitignore 中）
# 3. Gmail 需使用"應用專用密碼"，不是普通登入密碼
#    取得方式：Google 帳戶 → 安全性 → 兩步驗證 → 應用專用密碼
# 4. 生產環境部署时，在 Zeabur 控制台設定这些環境變數
